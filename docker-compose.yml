version: '3'

services:

  masterlash:
    image: node:latest
    volumes:
      - ./www:/www
    working_dir: /www
    command: bash -c "npm install && npm run dev"
    # command: bash -c "npm install && npm run prod"
    ports:
      - "80:3000"
